appId: com.anonymous.APP22
---
# E2E Test: Login Flow
# Tests the app can handle navigation and input

- launchApp
- extendedWaitUntil:
    visible:
      text: ".*"
    timeout: 5000

# Test app responsiveness
- scroll
- assertVisible:
    id: ".*"

# Login flow test passed!
