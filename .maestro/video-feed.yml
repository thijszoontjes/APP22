appId: com.anonymous.APP22
---
# E2E Test: Video Feed Interaction
# Tests video browsing and interaction on home screen

- launchApp
- assertVisible: "Ontdek*"

# Wait for video feed to load
- waitForAnimationToEnd:
    timeout: 5000

# Test scrolling through video feed
- scroll

# Verify video player components are present
# Note: Actual video playback requires backend connectivity
- assertVisible:
    id: ".*" # Verify basic UI structure loaded

# Test navigation back to top
- scrollUp
